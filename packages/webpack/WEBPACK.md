# Webpack / Next.js integration (`@seedprotocol/webpack`)

**Experimental — not supported. Do not depend on or publish this package.**

Prefer `withSeed` from `@seedprotocol/sdk/node`:

```js
import { withSeed } from '@seedprotocol/sdk/node'
```

This package remains in the monorepo for local experimentation only (`"private": true`).
