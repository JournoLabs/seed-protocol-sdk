/**
 * Test setup file
 * 
 * Platform classes are now set via real client initialization (platformClassesInit actor)
 * rather than using mocks. Tests should use setupTestEnvironment() from test-utils/client-init.ts
 * to initialize real environments.
 */
