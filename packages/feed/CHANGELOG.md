## 0.5.0

### Changed

- Depends on `@seedprotocol/eas` and `@seedprotocol/arweave` instead of `@seedprotocol/sdk`.
- `initializeSeedClient` now registers lean EAS/Arweave Node platforms (no full SDK client init).
