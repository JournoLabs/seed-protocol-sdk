# Monorepo Package Management

> **Note:** `@seedprotocol/cli` is **experimental and private**. It is not supported, not synced by `sync-versions`, and must not be published. Prefer working against `@seedprotocol/sdk` directly.

This document is historical context for how the CLI package was structured relative to the SDK. Treat it as reference only.
